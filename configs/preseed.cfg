# TAKERMAN AI SERVER - Minimal Debian Configuration
# Automated installation for high-performance AI server with NVIDIA GPU support

# Localization
d-i debian-installer/locale string en_US.UTF-8
d-i keyboard-configuration/xkb-keymap select us
d-i console-setup/ask_detect boolean false

# Network configuration
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string dev-ai
d-i netcfg/get_domain string dev-ai
d-i netcfg/wireless_wep string

# Mirror settings
d-i mirror/country string manual
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# Skip regular user creation (root-only system)
d-i passwd/make-user boolean false
d-i user-setup/allow-password-weak boolean true

# Clock and time zone setup
d-i clock-setup/utc boolean true
d-i time/zone string UTC
d-i clock-setup/ntp boolean true

# Partitioning
# Allow user to choose from physical disks (no LVM)
d-i partman-auto/method string regular
d-i partman-lvm/device_remove_lvm boolean true
d-i partman-md/device_remove_md boolean true

# Partition layout - use atomic recipe for simple physical partitions
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true

# Force user interaction for physical disk selection and partitioning confirmation
# This will show physical disks (sda, sdb, sdc, etc.) for user selection
d-i partman/confirm boolean false
d-i partman/confirm_nooverwrite boolean false

# Base system installation
d-i base-installer/install-recommends boolean false
d-i base-installer/kernel/image string linux-image-amd64

# Package selection - Complete system with all required packages
tasksel tasksel/first multiselect standard, ssh-server

# Essential system packages from default Debian repositories
d-i pkgsel/include string \
    openssh-server openssh-client \
    curl wget git rsync \
    build-essential ca-certificates gnupg gnupg2 \
    lsb-release apt-transport-https software-properties-common dirmngr \
    apt-utils debconf-utils \
    sudo bash-completion \
    vim nano tree less mc \
    screen tmux \
    zip unzip \
    net-tools iputils-ping traceroute dnsutils \
    iftop iotop htop \
    nmap netcat-openbsd tcpdump wireshark-common \
    ethtool bridge-utils vlan ifupdown \
    systemd-networkd systemd-resolved \
    ufw fail2ban \
    logrotate cron at \
    python3 python3-pip python3-venv \
    man-db manpages info \
    locate mlocate \
    psmisc procps lsof \
    strace ltrace \
    file whois \
    telnet ftp ncftp lftp \
    openssl \
    iperf3 \
    lm-sensors smartmontools \
    firmware-linux firmware-linux-nonfree \
    linux-headers-amd64 \
    dkms \
    openvpn easy-rsa

d-i pkgsel/upgrade select full-upgrade
popularity-contest popularity-contest/participate boolean false

# Install recommended packages for better system completeness  
d-i base-installer/install-recommends boolean true

# Skip additional language packages to save space
d-i pkgsel/install-language-support boolean false

# Boot loader installation
d-i grub-installer/only_debian boolean true
d-i grub-installer/with_other_os boolean true
d-i grub-installer/bootdev string default

# Finishing up the installation
d-i finish-install/reboot_in_progress note

# Custom commands to run after installation
d-i preseed/late_command string \
    in-target mkdir -p /tmp/custom-install /root/.takerman; \
    cp /cdrom/scripts/* /target/tmp/custom-install/; \
    cp /cdrom/docker-compose.yml /target/tmp/custom-install/; \
    cp /cdrom/branding/* /target/root/.takerman/ 2>/dev/null || true; \
    cp /target/tmp/custom-install/first_boot_setup.sh /target/root/.takerman/; \
    in-target chmod +x /tmp/custom-install/*.sh; \
    in-target chmod +x /root/.takerman/first_boot_setup.sh; \
    in-target /tmp/custom-install/post_install.sh; \
    in-target systemctl enable takerman-ai-setup.service; \
    in-target systemctl disable networking; \
    in-target systemctl enable systemd-networkd; \
    echo "TAKERMAN AI SERVER" > /target/etc/hostname

# Skip final message
d-i finish-install/keep-consoles boolean true